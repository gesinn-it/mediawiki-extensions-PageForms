*~
*.kate-swp
.*.swp

composer.lock
composer.phar

.idea/
node_modules/
vendor/
extensions/

.eslintcache
.phpunit.result.cache
.nyc_output
coverage
.phpcov-tool/

# Local DCI volume-mount override — not committed, survives submodule updates
build/docker-compose.override.yml
.env

# Local DCI custom extensions — not committed, only for local development
extensions.local.json
install-extensions.sh