*~
*.kate-swp
.*.swp

composer.lock
composer.phar

.idea/
node_modules/
vendor/
extensions/

.eslintcache
.phpunit.result.cache
.nyc_output
coverage
.phpcov-tool/

# Local DCI volume-mount override — not committed, survives submodule updates
build/docker-compose.override.yml
.env

# Local DCI custom extensions helper script — not committed, provided by the base image
install-extensions.sh
